
# Release 2.1.9 (2026-06-04)

## Features

- **declarative apt_repos + vagrant_plugins provisioning; .vergil/ is scratch (#105)**
Replace the PROVISION_HOOK param + script execution with declarative APT_REPOS (key + apt source line, registered with signed-by) and VAGRANT_PLUGINS (installed for the Lima user). The template owns the install; repos supply data, never a script. e2e now asserts an apt repo registers + a package layers. .vergil/ added to .gitignore as global scratch. Spec amended. Tracks #105 / vergil-tooling#1381.
