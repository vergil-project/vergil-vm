# >>> vergil-managed: base (managed by vrg-gitignore-sync; do not edit) >>>
*.swp
*.swo
*~
*.bak
.idea/
.vscode/
.DS_Store
Thumbs.db
.env
.env.*
*.log
.venv/
.worktrees/
.vergil/
.superpowers/
.claude/scheduled_tasks.lock
.claude/settings.local.json
build/
dist/
coverage.xml
junit.xml
licenses.json
docs/site/site/
node_modules/
# <<< vergil-managed <<<

# Secrets / env

# Vergil
# .vergil/ is Vergil scratch only (PR-template staging, *.local.toml overrides,
# work-in-progress). Nothing source-controlled lives here — see vergil-vm#105.

# Build output

# Python

# Docs site

# Go

# C / native objects

# Lima build artifacts
*.qcow2
*.raw
*.img
*.iso

# OpenTofu local scratch (off-platform backend, #199). Keep .terraform.lock.hcl.
opentofu/**/.terraform/
opentofu/**/*.tfstate
opentofu/**/*.tfstate.*
